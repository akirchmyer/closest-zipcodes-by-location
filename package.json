{
  "name": "closest-zipcodes-by-location",
  "version": "1.0.0",
  "description": "Find the closest zip codes by a given location",
  "main": "dist/index.js",
  "dependencies": {
    "geolib": "^2.0.21"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-stage-2": "^6.17.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "csvtojson": "^1.0.3",
    "del": "^2.2.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "node scripts/sync",
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "browserify src/index.js -o dist/index.js -t [ babelify --presets [ es2015 ] ]"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akirchmyer/closest-zipcodes-by-location.git"
  },
  "keywords": [
    "zip",
    "codes",
    "node"
  ],
  "author": "Andrew Kirchmyer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/akirchmyer/closest-zipcodes-by-location/issues"
  },
  "homepage": "https://github.com/akirchmyer/closest-zipcodes-by-location#readme",
  "babel": {
    "presets": ["es2015", "stage-2"]
  }
}
